<div id="new-nonprofit-form">
  <%= form_for @nonprofit, remote: true do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
    <%= f.submit(class: "btn btn-primary") %>
  <% end %>
</div>
